import React from "react";

const CurrencyOptions = () => {
  return (
    <React.Fragment>
      <select>
        <option>USD</option>
      </select>
    </React.Fragment>
  );
};

export default CurrencyOptions;
